# YOLO 训练自动化工作流改造 — Claude Code 执行任务书

## 0. 任务背景

将本仓库现有的 YOLO 目标检测训练流程（`train.py` / `val.py` 当前为硬编码参数）改造为「一句自然语言启动实验」的 Claude Code Agent 编排工作流。原始草案（用户提供）经过架构评审后存在若干设计错误，本文档是**修正后的最终执行版本**。

> **核心原则**：执行任何写操作前，先按 §1 探查环境、按 §2 向用户确认未知项。不要假设原方案的所有细节都正确——§3 列出已修正的设计错误和不可改的架构决策。

---

## 1. 执行前必须探查（你自己完成，不要问用户）

按顺序执行，把汇总结果发给用户后再继续：

1. `cat train.py` 和 `cat val.py` —— 确认现有参数和训练逻辑
2. `ls .claude/agents/ && ls .claude/` —— 列出已有 agent 和配置
3. `cat .claude/agents.json`（如存在）—— 判断是否需要清理
4. `cat .claude/agents/data-inspector.md`（如存在）—— 了解数据审计 agent 的接口
5. `pip show ultralytics | head -5` —— 确认版本
6. `ls ultralytics/cfg/models/improve/ 2>/dev/null | head -20` —— 确认 improve YAML 仓库是否存在及其规模
7. `find . -name "wandb" -o -name ".wandb" 2>/dev/null | head -5` —— 确认 wandb 是否已配置过
8. `nvidia-smi --query-gpu=index,name,memory.total --format=csv,noheader` —— 确认 GPU 数量和型号
9. `git log -1 --oneline` —— 确认仓库可写、git 可用

汇总后向用户报告：哪些假设成立、哪些不成立、哪些需要他来回答。

---

## 2. 必须向用户确认的事项

如果 §1 探查无法回答以下问题，必须直接问用户，**不要自行决定**：

1. `data-inspector` agent 的「8 步审计」具体是哪 8 步（如该 agent 存在但 .md 没写清楚）
2. `.claude/agents.json` 是否可以直接删除（如存在）
3. `improve/` 目录的来源：自加还是 fork 自某个开源项目（影响 `--cfg + .pt` 加载兼容性的处理）
4. wandb 的 entity / project 名称
5. dataset 配置文件路径（默认 `dataset/data.yaml`，需确认）

---

## 3. 架构决策（已锁定，不可改）

| 决策项 | 取值 |
|------|------|
| 训练运行模式 | **detach 启动**（nohup） + 主动查询；trainer agent 启动后立即返回 |
| 实验追踪 | 接入 **wandb**，同时维护本地 `experiments/registry.csv` 作为兜底 |
| GPU 策略 | **单卡为主**；多卡仅在用户显式指定 `--device 0,1` 时透传给 ultralytics（不做自动 DDP 决策） |
| Agent 拆分 | **trainer / monitor / evaluator** 三个独立 agent；不存在「常驻监控」agent |
| 配置形式 | 自然语言对话 + argparse CLI；**不引入 YAML 实验配置文件** |

### 已修正的原方案设计错误（不要回退到原方案）

| 原方案 | 修正后 |
|------|------|
| Agent 后台监控 + ScheduleWakeup | detach + monitor agent 主动查询（Claude Code 没有定时唤醒机制） |
| 冒烟判定「epoch 2 loss < epoch 1」 | 「无 NaN/Inf + 无 OOM + 完成 2 epoch + box_loss 上升不超 50%」 |
| `agents.json` + `agents/*.md` 双轨 | 统一到 `.claude/agents/*.md` |
| 缺少实验聚合 | 增加 `experiments/registry.csv` |
| GPU 检测仅看显存 | 显存空闲 + utilization < 20% + 无他人进程 三维判断 |
| 「自定义 Trainer 1 行改动」 | 删除该说法，自定义 Trainer 涉及多个 hook，不做承诺 |

---

## 4. 改造任务清单

### 4.1 改造 `train.py`

**约束**：
- **保持向后兼容**：`python train.py`（无参数）行为与改造前完全一致
- 使用 argparse，不引入第三方配置库
- 所有现有硬编码值作为 default

**新增参数**（按重要性排序）：

```
--model        权重或 YAML 路径 (default: 当前硬编码值)
--cfg          模型架构 YAML（improve 变体用，可选）
--data         数据集配置 (default: dataset/data.yaml)
--device       GPU ID，支持 "0" 或 "0,1" (default: "0")
--epochs       (default: 当前值)
--batch        (default: 当前值)
--imgsz        (default: 640)
--workers      (default: 0)
--optimizer    (default: SGD)
--patience     (default: 50)
--name         实验名 (default: exp)
--project      输出目录 (default: train)
--cos-lr       flag
--resume       flag
--amp / --no-amp
--cls-loss     字符串
--iou-loss     字符串
--cache        字符串
--wandb-project / --wandb-entity   传给 wandb
--dry-run      flag：只打印解析后的完整参数和将执行的命令，不真训练
```

**关键逻辑**：

1. **`--cfg` 与 `--model` 的组合处理**：
   - 仅 `--model xxx.pt`：`YOLO('xxx.pt')` 直接训练
   - `--model xxx.pt --cfg improve/yyy.yaml`：`YOLO('improve/yyy.yaml').load('xxx.pt')`
   - **必须显式打印权重加载报告**：遍历 `state_dict`，对比 shape，输出三栏表格（layer_name / loaded / skipped_reason），让用户看见到底加载了多少层。架构改动会让权重静默丢失，这是常见坑
   - 仅 `--model xxx.yaml`：从零训练

2. **wandb 集成**：
   - 用 ultralytics 原生支持：`from ultralytics import settings; settings.update({'wandb': True})`
   - 把 `git rev-parse HEAD`、`--cfg` 路径、`--data` 路径、所有超参写入 wandb config

3. **registry 写入**：
   - 训练真正启动前（dry-run 不写）追加一行到 `experiments/registry.csv`，schema 见 §4.6
   - `status=started`，metrics 字段留空

### 4.2 改造 `val.py`

**新增参数**：

```
--model    权重路径 (default: 当前值)
--data     (default: dataset/data.yaml)
--device   (default: 0)
--batch    (default: 16)
--imgsz    (default: 640)
--split    val/test (default: test)
--name     实验名（用于回写 registry，可选）
```

**关键逻辑**：评估完成后，如果 `--name` 非空且 registry 中存在该行 → **update 该行**的 `best_mAP50` / `best_mAP50_95` / `status=evaluated`。用 pandas 读改写即可，量小不需要数据库。

### 4.3 新建 `.claude/agents/trainer.md`

**职责边界**：从用户说「训练 X」开始，到 `nohup python train.py ... &` 启动并返回 PID 为止。**不做训练中监控、不做评估**。

**工作流**：

#### 阶段 0 — GPU 检测
```bash
nvidia-smi --query-gpu=index,memory.used,memory.total,utilization.gpu --format=csv,noheader,nounits
nvidia-smi --query-compute-apps=pid,used_memory,gpu_uuid --format=csv,noheader
```
- 「真空闲卡」判定：`memory.used / memory.total < 20%` AND `utilization < 20%` AND 无他人训练进程
- 选最优卡 → 报告「GPU N 空闲 XGB/YGB，util Z%，建议使用」→ 用户可覆盖
- 解析失败 → 回退 `device=0` + 警告

#### 阶段 1 — 一次性参数确认
- **一条消息问完所有可变项**：模型规模、epochs、batch、imgsz、实验名、是否注入模块（默认无）
- 给所有项默认值，用户回车即可
- 如果用户提了模块（如「加 EMA」）→ 在 `ultralytics/cfg/models/improve/attention/yolov8/` 搜索匹配 YAML，列出全部变体并解释差异（变体编号通常对应不同插入位置），让用户选

#### 阶段 2 — 数据审计
- 调用 `data-inspector` subagent（如果存在）。返回 PASS → 继续；FAIL → 中止并展示错误
- 如果 data-inspector 不存在 → 跳过本阶段并**明确告知**用户「未做数据审计」

#### 阶段 3 — 冒烟测试
```bash
python train.py --model {weight} --cfg {cfg} --device {gpu} --epochs 2 \
  --batch {batch} --imgsz {imgsz} --name smoke_{name} --project smoke
```

PASS 标准（**全部**满足）：
- 进程退出码 0
- `smoke/smoke_{name}/results.csv` 至少 2 行
- stdout 无 NaN / Inf
- stdout 无 `CUDA out of memory`
- `box_loss` 第二行相对第一行增长 ≤ 50%（不要求严格下降）

FAIL 处理：
- OOM → batch 减半重试，**最多 2 次**（如 16→8→4）；每次重试在 registry 标注 `smoke_retry_n`
- NaN → 提示用户加 `--no-amp` 或降学习率，停止
- 其他 → 展示 stderr 最后 30 行给用户，停止

#### 阶段 4 — detach 启动正式训练
```bash
mkdir -p logs
nohup python train.py --model {weight} --cfg {cfg} --device {gpu} \
  --epochs {epochs} --batch {batch} --imgsz {imgsz} --name {name} \
  --wandb-project {wandb_project} \
  > logs/{name}.log 2>&1 &
echo $! > logs/{name}.pid
```

返回给用户：
```
✅ 训练已启动
  PID:        {pid}
  日志:       logs/{name}.log
  权重输出:   train/{name}/weights/
  wandb URL:  （sleep 10 后从日志 grep 出来；抓不到就标 N/A）
  查询进度:   说「看下 {name} 进度」
```

**然后 trainer agent 任务结束，不要尝试 sleep / poll**。

### 4.4 新建 `.claude/agents/monitor.md`

**职责**：用户说「看下 {name} 进度」/「现在跑得怎么样」时被调用。

**工作流**：
1. 确定目标实验：用户给了 name → 直接查；否则读 `experiments/registry.csv` 找 `status=started` 的最近一条
2. 检查进程：`ps -p $(cat logs/{name}.pid)`
   - 进程存活 → 进入 3
   - 进程不在 → 读最后 30 行日志判断是正常完成还是崩溃；崩溃则更新 registry `status=crashed`
3. 读 `train/{name}/results.csv` 最后一行 → 当前 epoch / 总 epoch、box_loss、cls_loss、mAP50（如已开始 val）
4. 估算剩余时间：从 results.csv 时间戳推平均每 epoch 时长 × 剩余 epoch
5. 输出格式化报告 + wandb URL（如有）

**不要**主动建议用户操作（停止、调参等），只汇报状态。

### 4.5 新建 `.claude/agents/evaluator.md`

**职责**：训练结束后用户说「评估 {name}」/「val 一下」时调用。

**工作流**：
1. 找权重：`train/{name}/weights/best.pt`（不存在则用 `last.pt` 并提示）
2. 跑 `python val.py --model {权重} --device {gpu} --name {name}`
3. 解析输出的 mAP 表格（按类别）
4. 横向对比**仅在用户主动要求时做**（「跟之前的比较一下」），按 mAP50-95 排序
5. 给建议时**保持克制**：只输出有数据支撑的判断（如「bus 类样本 < 50 且 AP < 0.3，建议补数据」），不要泛泛而谈

### 4.6 新建 `experiments/registry.csv`

首次创建写入表头：

```
exp_name,timestamp,git_commit,model,cfg,data,epochs,batch,imgsz,gpu,status,best_mAP50,best_mAP50_95,log_path,wandb_url,notes
```

`status` 取值：`started` / `smoke_failed` / `crashed` / `completed` / `evaluated`

操作约定：
- trainer 阶段 4 启动训练时**追加一行**（`status=started`，metrics 留空）
- evaluator 完成后 **update 该行**（不新增）
- monitor 检测到崩溃时 update `status=crashed`
- 文件并发写入风险：研究项目单人使用，暂不加锁；如果未来多人共用再换 sqlite

### 4.7 更新 `CLAUDE.md`

追加一节「训练自动化工作流」，包含：
- 三个 agent 的触发方式（用户用什么自然语言能调起）
- registry.csv 的位置和字段
- 启动新实验的标准句式示例
- 多卡使用方法（`--device 0,1`）
- **已知限制**：trainer agent 启动后不再主动汇报，需用户主动喊 monitor

### 4.8 清理 `.claude/agents.json`

如果 §1 探查发现该文件存在且只是早期 prompt 集合：
1. 备份到 `.claude/agents.json.bak`
2. 删除原文件
3. 在 CLAUDE.md 中说明「subagents 配置统一在 `.claude/agents/*.md`」

如果该文件被某个 hook / 脚本依赖：保留，但停止写入新内容，并向用户说明。

### 4.9 [可选] 新建 `.claude/commands/train.md`

提供 slash command `/train`，例如：
```
/train yolov8s --epochs 100 --module EMA
```
比纯自然语言更可控（不依赖意图识别）。command 内部触发 trainer agent。**时间紧可跳过**。

---

## 5. 设计约束（贯穿所有改造）

- **不要捏造工具**：Claude Code 的 subagent 是单次调用、无定时唤醒、无后台常驻。任何依赖「定时器 / 后台 hook / 长期监听」的设计都要被拒绝
- **不要静默失败**：所有自动决策（GPU 选择、batch 减半、跳过 data-inspector）必须显式告知用户
- **不要扩大改动范围**：如果发现 train.py 里有看起来可以重构的代码，**不要重构**。本任务只做 argparse 化、wandb 集成、registry 写入
- **保持向后兼容**：所有 `python train.py` / `python val.py` 无参数调用必须行为不变
- **不要引入新依赖**：除 wandb（已确认接入）外不加新包
- **不要在 agent prompt 里写训练超参的「专家建议」**：这容易过时且不准。agent 只做编排，调参由用户决定

---

## 6. 验收标准

逐条手动验证，每条通过再做下一项：

- [ ] `python train.py --help` 列出所有新参数
- [ ] `python train.py`（无参数）行为与改造前一致（git stash 对比一个 epoch 的 results.csv）
- [ ] `python train.py --model yolov8n.pt --device 0 --epochs 1 --batch 2 --dry-run` 打印命令但不训练
- [ ] `python train.py --model yolov8s.pt --cfg improve/attention/yolov8/yolov8-EMA-1.yaml --epochs 1` 能跑且打印权重加载报告
- [ ] `python val.py --model train/xxx/weights/best.pt --device 0 --name xxx` 完成后 registry.csv 中 xxx 行的 mAP 字段被填充
- [ ] 在 Claude Code 中说「训练 yolov8s 100 epochs」能触发 trainer agent 走完阶段 0–4
- [ ] 训练启动后**关闭 Claude Code 会话、重新打开**、说「看下 xxx 进度」能触发 monitor agent 正确报告
- [ ] wandb dashboard 能看到 run，config 里包含 git_commit / cfg 路径

---

## 7. 交付顺序

务必按此顺序，每完成一项让用户确认再做下一项：

1. §1 探查 → 汇报结果
2. §2 向用户问清楚未知项
3. 改 `train.py`（含 dry-run，便于后续调试）
4. 改 `val.py`
5. 建 `experiments/registry.csv`
6. **端到端用 CLI 跑通**：1 epoch 训练 + val（不经过 agent，先验证脚本本身没问题）
7. 建 `trainer.md` → 用户测试一句话训练
8. 建 `monitor.md` → 用户测试查询
9. 建 `evaluator.md` → 用户测试评估
10. 更新 `CLAUDE.md`
11. 清理 `agents.json`
12. （可选）建 `/train` command

每一步出错时**停下来问用户**，不要自己尝试激进修复。